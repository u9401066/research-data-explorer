name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pre-commit

      - name: Ruff check
        run: python -m ruff check .

      - name: Ruff format check
        run: python -m ruff format --check .

      - name: Governance and regression tests
        run: python3 -m pytest -q -m "not vendor_integration"

      - name: Pre-commit hooks
        run: SKIP=no-commit-to-branch pre-commit run --all-files

  extension-quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode-extension
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: vscode-extension/package-lock.json

      - name: Install extension dependencies
        run: npm ci

      - name: Lint extension
        run: npm run lint

      - name: Test extension
        run: npm test

      - name: Package VSIX
        run: npm run package -- --no-yarn

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: research-data-explorer-vsix
          path: vscode-extension/*.vsix
          if-no-files-found: error

  extension-cross-platform-smoke:
    name: VSIX cross-platform smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: vscode-extension
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-13, macos-14]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: vscode-extension/package-lock.json

      - name: Install extension dependencies
        run: npm ci

      - name: Check bundled asset contract
        run: npm run sync-assets:check

      - name: Compile extension
        run: npm run compile

      - name: Run cross-platform helper tests
        run: npm test -- test/utils.test.ts test/extensionHelpers.test.ts test/uvManager.test.ts

      - name: Check bundled Python install shape
        run: npm run test:install-smoke

      - name: Package VSIX
        run: npm run package -- --no-yarn

      - name: Validate packaged extension
        run: npm run validate -- --skip-tests

  vendor-contract:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Start vendor services
        run: docker compose -f vendor/automl-stat-mcp/docker-compose.yml --profile ml up -d

      - name: Run vendor integration test
        run: python3 -m pytest -q tests/test_vendor_automl_contract_integration.py -m vendor_integration

      - name: Stop vendor services
        if: always()
        run: docker compose -f vendor/automl-stat-mcp/docker-compose.yml --profile ml down -v
