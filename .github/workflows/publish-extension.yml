name: Publish Extension

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      publishMarketplace:
        description: Publish to Visual Studio Marketplace
        required: false
        default: true
        type: boolean
      publishOpenVsx:
        description: Publish to Open VSX
        required: false
        default: true
        type: boolean

jobs:
  python-quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
          cache-dependency-glob: pyproject.toml
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: uv sync --extra dev
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run pytest -q -m "not vendor_integration"
      - run: uv run python scripts/audit_mcp_surface.py
      - run: uv build
      - uses: actions/upload-artifact@v4
        with:
          name: python-distributions
          path: dist/*
          if-no-files-found: error

  cross-platform-smoke:
    name: VSIX cross-platform smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: vscode-extension
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-15-intel, macos-14]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: vscode-extension/package-lock.json

      - name: Install extension dependencies
        run: npm ci

      - name: Check bundled asset contract
        run: npm run sync-assets:check

      - name: Compile extension
        run: npm run compile

      - name: Run cross-platform helper tests
        run: npm test -- test/utils.test.ts test/extensionHelpers.test.ts test/uvManager.test.ts

      - name: Check bundled Python install shape
        run: npm run test:install-smoke

      - name: Package VSIX
        run: npm run package -- --no-yarn

      - name: Validate packaged extension
        run: npm run validate -- --skip-tests

  package:
    needs: [python-quality, cross-platform-smoke]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode-extension
    outputs:
      vsix_name: ${{ steps.package.outputs.vsix_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: vscode-extension/package-lock.json

      - name: Install extension dependencies
        run: npm ci

      - name: Lint extension
        run: npm run lint

      - name: Test extension
        run: npm test

      - name: Package VSIX
        id: package
        shell: bash
        run: |
          npm run package -- --no-yarn
          vsix_name=$(basename *.vsix)
          echo "vsix_name=${vsix_name}" >> "$GITHUB_OUTPUT"

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: packaged-vsix
          path: vscode-extension/*.vsix
          if-no-files-found: error

      - name: Set up Python for payload verification
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Download tested Python distributions
        uses: actions/download-artifact@v4
        with:
          name: python-distributions
          path: dist

      - name: Gate all publishing targets on payload integrity
        run: python ../scripts/audit_release_artifacts.py

  github-release:
    needs: package
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/download-artifact@v4
        with:
          name: packaged-vsix
          path: vscode-extension
      - uses: actions/download-artifact@v4
        with:
          name: python-distributions
          path: dist
      - name: Check release payload against source
        run: python scripts/audit_release_artifacts.py
      - name: Publish verified GitHub release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" vscode-extension/*.vsix dist/* --verify-tag --generate-notes --title "RDE $GITHUB_REF_NAME"

  publish-marketplace:
    needs: package
    if: github.event_name == 'push' || inputs.publishMarketplace
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode-extension
    env:
      VSCE_PAT: ${{ secrets.VSCE_PAT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: vscode-extension/package-lock.json

      - name: Install extension dependencies
        run: npm ci

      - name: Download VSIX artifact
        uses: actions/download-artifact@v4
        with:
          name: packaged-vsix
          path: vscode-extension

      - name: Skip marketplace publish when token is unavailable
        if: env.VSCE_PAT == ''
        run: echo "VSCE_PAT is not set; skipping Visual Studio Marketplace publish."

      - name: Publish to Visual Studio Marketplace
        if: env.VSCE_PAT != ''
        run: npx @vscode/vsce publish --packagePath "${{ needs.package.outputs.vsix_name }}" --no-yarn

  publish-openvsx:
    needs: package
    if: github.event_name == 'push' || inputs.publishOpenVsx
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode-extension
    env:
      OVSX_PAT: ${{ secrets.OVSX_PAT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: vscode-extension/package-lock.json

      - name: Install extension dependencies
        run: npm ci

      - name: Download VSIX artifact
        uses: actions/download-artifact@v4
        with:
          name: packaged-vsix
          path: vscode-extension

      - name: Skip Open VSX publish when token is unavailable
        if: env.OVSX_PAT == ''
        run: echo "OVSX_PAT is not set; skipping Open VSX publish."

      - name: Publish to Open VSX
        if: env.OVSX_PAT != ''
        run: npx ovsx publish "${{ needs.package.outputs.vsix_name }}" -p "$OVSX_PAT"
